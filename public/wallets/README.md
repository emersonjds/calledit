# Wallet logos

Drop the official brand logos here to replace the built-in inline fallbacks:

- `phantom.png`
- `metamask.png`

Square PNGs (recommended 128×128 or larger) work best; they render at 36×36 with
rounded corners. If a file is missing, the modal shows a simple inline SVG mark
instead, so the app never breaks.
